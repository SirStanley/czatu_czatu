<Window x:Class="CzatuCzatu.Views.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CzatuCzatu.Views"
        mc:Ignorable="d"
        Title="Witamy w Czatu-Czatu" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Assets/favicon.ico"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernWelcomeButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#F7F9FC">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <Image Source="pack://application:,,,/Assets/favicon.ico" 
                   Width="85" Height="85" 
                   Margin="0,0,0,20" />

            <TextBlock Text="Witamy w czatu-czatu" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#333" 
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Twoje bezpieczne miejsce do rozmów" 
                       FontSize="14" 
                       Foreground="#777" 
                       Margin="0,5,0,40"
                       HorizontalAlignment="Center"/>

            <Button x:Name="BtnGoToLogin" 
                    Content="Zaloguj się" 
                    Style="{StaticResource ModernWelcomeButton}"
                    Click="BtnGoToLogin_Click"/>

            <Button x:Name="BtnGoToRegister" 
                    Content="Zarejestruj się" 
                    Style="{StaticResource ModernWelcomeButton}"
                    Click="BtnGoToRegister_Click"/>

        </StackPanel>
    </Grid>
</Window>